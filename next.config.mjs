/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'nnvvrwdezsncieasfxqg.supabase.co',
        port: '',
      },
    ],
  },
};

export default nextConfig;
